#!/usr/bin/env bash

function profile {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  PATH="$shome/bin:$shome/exec:$PATH"

  if [[ -d "/vagrant" ]]; then
    BASEBOX_CACHE="/vagrant"
  else
    BASEBOX_CACHE="$shome"
  fi

  export BASEBOX_CACHE
}

profile
